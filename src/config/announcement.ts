/** Flip `enabled` to false to hide the announcement bar site-wide. Copy lives
 * in the `announcement` dictionary key so it stays translatable. */
export const announcementConfig = {
  enabled: true,
  href: "/contact",
};
